# Core FastAPI Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Settings and Validation
pydantic>=2.9.0
pydantic[email]>=2.9.0
pydantic-settings>=2.5.0

# Database & ORM
sqlalchemy>=2.0.35
asyncpg>=0.29.0
alembic>=1.13.3

# Security & Auth
pyjwt>=2.9.0
bcrypt>=4.2.0

# Testing & Client
httpx>=0.27.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
aiosqlite>=0.20.0

# AI Provider SDKs (Step 5A)
# google-genai: Official Gemini SDK (required only when Gemini is the active provider)
google-genai>=1.0.0

# openai: Official OpenAI SDK (also compatible with Groq, OpenRouter, Cerebras)
# Required only when OpenAI/Groq/OpenRouter is the active provider
openai>=1.0.0

# Medical Document Processing, OCR & Storage (Step 7)
python-multipart>=0.0.18
pypdf>=5.0.0
pillow>=10.0.0
pytesseract>=0.3.10
